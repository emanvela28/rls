services:
  - type: web
    name: mercor-dashboard
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.server:app --host 0.0.0.0 --port $PORT

cron:
  - name: refresh-data
    schedule: "*/3 * * * *"
    command: python3 src/scrape.py
